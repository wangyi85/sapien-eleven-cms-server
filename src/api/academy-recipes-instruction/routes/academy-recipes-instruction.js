'use strict';

/**
 * academy-recipes-instruction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::academy-recipes-instruction.academy-recipes-instruction');
